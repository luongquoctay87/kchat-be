spring:
  config:
    activate:
      on-profile: dev

logging:
  level:
    com.kchat: DEBUG
    org.springframework.web: INFO
    org.hibernate.SQL: WARN

kchat:
  webrtc:
    # Emulator↔emulator needs TURN; openrelay only for local profile.
    allow-dev-openrelay: true
